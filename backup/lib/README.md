# Backup Libraries

- `archive.sh` contains the active Phase 2 compression and checksum helpers.
- `encrypt.sh`, `upload_ftp.sh`, `database.sh`, and `retention.sh` are inert
  placeholders for later phases and are not sourced by `backup.sh`.
