# Infrastructure inventory

Inventoried: [VPS1](vps1.md), [VPS2](vps2.md), the
[local report-delivery/application host](sdrc-report-delivery.md), the
[Ctrl-S legacy Shivam server](ctrls-legacy-shivam.md), and an initial
[Orthanc/DICOM discovery record](orthanc-dicom.md).

Still required: remaining local application servers, legacy Oracle/LIMS,
Mirth, analysers/interfaces, network/firewall/VPN, NVR/cameras, and the current
attendance/ERPNext host and data flow. Create
one file per machine or managed service using `templates/server.md`; do not put
secrets in inventory files.
